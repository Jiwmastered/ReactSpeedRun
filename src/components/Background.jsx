const backgroundSet = {
    solid : {
        
    },
    
}

function Background(props) {
    const type = props.type;
    const config = props.config;

        

    return (
        <div>
            
        </div>
    )
}

export default Background;